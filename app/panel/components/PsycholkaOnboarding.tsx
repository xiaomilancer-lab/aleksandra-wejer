"use client";
import { CalendarDays, Heart, Leaf, Sparkles } from "lucide-react";
import { useCallback, useEffect, useState } from "react";
import PsycholkaGreetingSequence from "./PsycholkaGreetingSequence";
import PsycholkaWidget from "./PsychOLKAWidget";
const storageKey = "psycholka-onboarding-seen"; type Step = "welcome" | "cards" | "finish";
export default function PsycholkaOnboarding() { const [isOpen, setIsOpen] = useState(false); const [step, setStep] = useState<Step>("welcome"); const [greetingReady, setGreetingReady] = useState(false); useEffect(() => { const openOnboarding = window.setTimeout(() => { const force = new URLSearchParams(window.location.search).get("onboarding") === "1"; if (force || window.localStorage.getItem(storageKey) !== "true") setIsOpen(true); }, 0); return () => window.clearTimeout(openOnboarding); }, []); const completeGreeting = useCallback(() => setGreetingReady(true), []); const finish = () => { window.localStorage.setItem(storageKey, "true"); setIsOpen(false); window.history.replaceState({}, "", "/panel"); }; if (!isOpen) return null; return <div className="fixed inset-0 z-50 overflow-y-auto bg-[#F8F5F0]/95 p-4 backdrop-blur-sm"><main className="psycholka-onboarding mx-auto flex min-h-full max-w-2xl flex-col items-center justify-center py-8 text-center">{step === "welcome" ? <PsycholkaGreetingSequence onComplete={completeGreeting} /> : <PsycholkaWidget context={step === "finish" ? "success" : "welcome"} action="wave" className="psycholka-onboarding-widget" />}{step === "welcome" && <><h1 className="mt-4 text-3xl font-bold text-[#2D4739] sm:text-4xl">Dobrze, że jesteś.</h1><p className="mt-6 whitespace-pre-line text-base leading-relaxed text-gray-700 sm:text-lg">{"Cześć!\n\nJestem PsychOLKA.\n\nBędę towarzyszyć Ci podczas korzystania z aplikacji.\n\nNie jestem psychologiem.\nNie zastępuję Aleksandry.\n\nJestem tutaj, aby wszystko było trochę prostsze i trochę spokojniejsze."}</p><button type="button" disabled={!greetingReady} onClick={() => setStep("cards")} className="mt-8 inline-flex min-h-12 items-center gap-2 rounded-xl bg-[#6D7A62] px-6 py-3 text-sm font-semibold text-white disabled:cursor-wait disabled:bg-[#AAB5A4]"><Leaf size={18} />Poznajmy się</button></>}{step === "cards" && <><h1 className="mt-6 text-3xl font-bold text-[#2D4739]">Krok po kroku</h1><div className="mt-7 grid w-full gap-4 sm:grid-cols-3"><Card icon={CalendarDays} text="Pomogę Ci odnaleźć się w aplikacji." /><Card icon={Heart} text={"Nie oceniam.\nNie krytykuję.\nPo prostu jestem."} /><Card icon={Leaf} text="Mały krok też jest krokiem." /></div><button type="button" onClick={() => setStep("finish")} className="mt-8 inline-flex min-h-12 items-center gap-2 rounded-xl bg-[#6D7A62] px-6 py-3 text-sm font-semibold text-white"><Sparkles size={18} />Dalej</button></>}{step === "finish" && <><Heart size={28} className="mt-6 text-[#C76E76]" fill="currentColor" /><h1 className="mt-3 text-3xl font-bold text-[#2D4739]">Dobrze, że jesteś.</h1><p className="mt-4 text-base text-gray-600">Zaczynamy spokojnie, krok po kroku.</p><button type="button" onClick={finish} className="mt-8 inline-flex min-h-12 items-center gap-2 rounded-xl bg-[#6D7A62] px-6 py-3 text-sm font-semibold text-white"><Heart size={18} fill="currentColor" />Zaczynamy</button></>}</main></div>; }
function Card({ icon: Icon, text }: { icon: typeof CalendarDays; text: string }) { return <article className="flex min-h-40 flex-col items-center justify-center rounded-3xl border border-[#E5E1D8] bg-white p-5 shadow-sm"><Icon size={24} className="text-[#6D7A62]" /><p className="mt-4 whitespace-pre-line text-sm leading-relaxed text-[#2D4739]">{text}</p></article>; }
